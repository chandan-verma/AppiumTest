package common;


public class Notes {
	// driver.executeScript("mobile: scroll", ImmutableMap.of("direction", "down"));
	// driver.executeScript("mobile:viewportScreenshot");
	// File scrFile = ((TakesScreenshot)driver).getScreenshotAs(OutputType.FILE);
	// Object a = driver.executeScript("mobile:batteryInfo");

	// driver.findElement(MobileBy.AndroidUIAutomator("new
	// UiSelector().resourceId(\"android:id/text1\").instance(8)"))
	// .click();

	// Most of the time 'class name' and 'type' know as tag name.

	// Alert alert = getWait().until(ExpectedConditions.alertIsPresent());
	// Alert alert = driver.switchTo().alert();

	// capabilities.setCapability(MobileCapabilityType.UDID, "2b423791");
	// capabilities.setCapability(MobileCapabilityType.FULL_RESET, "false");
	// capabilities.setCapability(MobileCapabilityType.NO_RESET, "true");
	// capabilities.setCapability("appPackage", "com.facebook.katana");
	// capabilities.setCapability("appActivity",
	// "com.miui.calculator.cal.CalculatorActivity");
	// capabilities.setCapability(MobileCapabilityType.PLATFORM_VERSION, "12.0.1");

	// capabilities.setCapability(MobileCapabilityType.APP,
	// "/Users/chandan.verma/eclipse-workspace/Appium/app/UI Framework
	// Catalog_v0.3.0_apkpure.com.apk");
	
	
	//taskkill /F /IM node.exe 
	
	// TODO
	// # soft assertion
	//auto start appium
	// no reset
	//jenkins
	// auto device start
}
